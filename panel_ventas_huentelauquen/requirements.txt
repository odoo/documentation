# El panel funciona SIN dependencias: usa sólo la biblioteca estándar de Python.
#
# Instale esto únicamente si va a publicarlo en producción y quiere un servidor
# WSGI robusto en lugar del servidor de desarrollo:
#
#   pip install -r requirements.txt
#   waitress-serve --host 0.0.0.0 --port 8069 --call panel_ventas.wsgi:crear_app
waitress>=3.0
